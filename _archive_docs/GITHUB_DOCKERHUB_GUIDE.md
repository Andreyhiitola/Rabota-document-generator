# 🚀 GitHub + Docker Hub - Полное руководство по развертыванию

## 📋 Оглавление

1. [Архитектура](#архитектура)
2. [Настройка GitHub Repository](#настройка-github-repository)
3. [Настройка Docker Hub](#настройка-docker-hub)
4. [Настройка GitHub Secrets](#настройка-github-secrets)
5. [GitHub Actions (CI/CD)](#github-actions-cicd)
6. [Развертывание на сервере](#развертывание-на-сервере)
7. [Обновление приложения](#обновление-приложения)

---

## 🏗️ Архитектура

```
╔══════════════════════════════════════════════════════════════╗
║              ПОЛНАЯ АРХИТЕКТУРА РАЗВЕРТЫВАНИЯ                ║
╚══════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────┐
│  1. РАЗРАБОТКА                                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ Локальная машина                                      │  │
│  │ • Пишете код                                          │  │
│  │ • Тестируете локально                                 │  │
│  │ • git commit + git push                               │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  2. GITHUB REPOSITORY                                       │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ • Хранение кода                                       │  │
│  │ • Версионирование                                     │  │
│  │ • GitHub Actions (автоматика)                         │  │
│  │ • Secrets (безопасное хранение ключей)                │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  3. GITHUB ACTIONS (автоматическая сборка)                  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ При push в main:                                      │  │
│  │ • Сборка Docker образа                                │  │
│  │ • Тестирование                                        │  │
│  │ • Публикация в Docker Hub                             │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  4. DOCKER HUB                                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ • Хранение Docker образов                             │  │
│  │ • Версионирование (tags)                              │  │
│  │ • Публичный/приватный доступ                          │  │
│  │ • yourname/severen-generator:latest                   │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  5. PRODUCTION СЕРВЕР                                       │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ • docker compose pull                                 │  │
│  │ • docker compose up -d                                │  │
│  │ • .env с секретами (локально)                         │  │
│  │ • Работающее приложение                               │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

**Важно:** 
- Код → GitHub
- Образы → Docker Hub
- Секреты → GitHub Secrets + локальный .env

---

## 📦 Настройка GitHub Repository

### Шаг 1: Создание репозитория

1. Откройте https://github.com
2. Нажмите "New repository"
3. Заполните:
   ```
   Repository name: severen-document-generator
   Description: Автоматизация документов с интеграцией Trello
   Visibility: Private (рекомендуется)
   ✓ Add a README file
   ✓ Add .gitignore (Python)
   License: MIT (опционально)
   ```
4. Нажмите "Create repository"

### Шаг 2: Клонирование и загрузка кода

```bash
# Клонируйте репозиторий
git clone https://github.com/ваш-username/severen-document-generator.git
cd severen-document-generator

# Скопируйте все файлы проекта (кроме .env!)
cp -r /path/to/your/project/* .

# Проверьте что .env НЕ добавлен
git status
# Должно быть: .env в .gitignore

# Добавьте файлы
git add .
git commit -m "Initial commit: Severen document generator"
git push origin main
```

### Шаг 3: Проверьте .gitignore

Убедитесь что `.gitignore` содержит:

```
# ВАЖНО: Никогда не коммитить эти файлы!
.env
.env.local
.env.*.local
config.local.py
secrets/
*_api_key*
*_token*

# Данные пользователя
output/
data/
excel_files/*.xlsx
```

---

## 🐳 Настройка Docker Hub

### Шаг 1: Создание аккаунта

1. Откройте https://hub.docker.com
2. "Sign Up" или войдите
3. Запомните ваш username (нужен для следующих шагов)

### Шаг 2: Создание репозитория

1. Нажмите "Create Repository"
2. Заполните:
   ```
   Repository Name: severen-generator
   Description: Document generator with Trello integration
   Visibility: Public или Private
   ```
3. Нажмите "Create"

**Результат:** `your-dockerhub-username/severen-generator`

### Шаг 3: Создание Access Token

1. Профиль → "Account Settings"
2. "Security" → "Access Tokens"
3. "New Access Token"
4. Заполните:
   ```
   Description: GitHub Actions
   Access permissions: Read, Write, Delete
   ```
5. **ВАЖНО:** Скопируйте токен! (показывается только один раз)

---

## 🔐 Настройка GitHub Secrets

GitHub Secrets - безопасное хранение ключей для CI/CD.

### Шаг 1: Добавление секретов

1. Откройте ваш GitHub репозиторий
2. Settings → Secrets and variables → Actions
3. Нажмите "New repository secret"

### Шаг 2: Добавьте следующие секреты

#### DOCKERHUB_USERNAME
```
Name: DOCKERHUB_USERNAME
Secret: ваш-username-в-docker-hub
```

#### DOCKERHUB_TOKEN
```
Name: DOCKERHUB_TOKEN
Secret: токен-из-docker-hub-созданный-выше
```

#### TRELLO_API_KEY (опционально, для тестов)
```
Name: TRELLO_API_KEY
Secret: ваш-trello-api-key
```

#### TRELLO_TOKEN (опционально, для тестов)
```
Name: TRELLO_TOKEN
Secret: ваш-trello-token
```

**Результат:** 
```
✓ DOCKERHUB_USERNAME
✓ DOCKERHUB_TOKEN
✓ TRELLO_API_KEY (опционально)
✓ TRELLO_TOKEN (опционально)
```

---

## ⚙️ GitHub Actions (CI/CD)

### Файл workflow уже создан!

Файл `.github/workflows/docker-publish.yml` автоматически:

```yaml
Что происходит при git push:
━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Checkout кода из GitHub
2. Настройка Docker Buildx
3. Логин в Docker Hub (используя Secrets)
4. Сборка Docker образа
5. Публикация в Docker Hub:
   • username/severen-generator:latest
   • username/severen-generator:main
   • username/severen-generator:v1.0.0 (если тег)
6. Обновление описания на Docker Hub
```

### Редактирование workflow

Откройте `.github/workflows/docker-publish.yml` и измените:

```yaml
env:
  DOCKER_IMAGE_NAME: severen-generator
  DOCKERHUB_USERNAME: your-dockerhub-username  # ← ИЗМЕНИТЕ!
```

**Замените** `your-dockerhub-username` на ваш реальный username.

### Проверка работы

После push в main:

1. Откройте GitHub → Actions
2. Увидите запущенный workflow
3. Дождитесь завершения (3-5 минут)
4. Проверьте Docker Hub - образ опубликован!

**Пример результата:**
```
✓ Build and Push to Docker Hub
  ✓ Checkout code
  ✓ Set up Docker Buildx
  ✓ Log in to Docker Hub
  ✓ Build and push Docker image
  ✓ Docker Hub Description
```

---

## 🖥️ Развертывание на сервере

### Вариант 1: С нуля (первый раз)

```bash
# 1. Установка Docker (если еще не установлен)
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo apt-get install docker-compose-plugin

# 2. Создание рабочей папки
mkdir ~/severen-app
cd ~/severen-app

# 3. Создание структуры папок
mkdir -p output excel_files data email_templates

# 4. Скачивание docker-compose.prod.yml
curl -O https://raw.githubusercontent.com/ваш-username/severen-document-generator/main/docker-compose.prod.yml
mv docker-compose.prod.yml docker-compose.yml

# 5. Создание .env файла
cat > .env << 'EOF'
# Docker Hub
DOCKERHUB_USERNAME=ваш-dockerhub-username

# Trello API
TRELLO_API_KEY=ваш-api-key
TRELLO_TOKEN=ваш-token
TRELLO_BOARD_ID=gfcln6rS

# Excel
EXCEL_PATH=/app/excel_files/ПРИЛОЖЕНИЕ_1_3_4_ALL_IN_ONE.xlsx

# Web
WEB_PORT=5000
FLASK_ENV=production
SECRET_KEY=$(openssl rand -hex 32)
EOF

# 6. Редактирование .env (вставьте реальные ключи)
nano .env

# 7. Положите Excel файл
# Скопируйте ПРИЛОЖЕНИЕ_1_3_4_ALL_IN_ONE.xlsx в excel_files/

# 8. Запуск
docker compose pull
docker compose up -d

# 9. Проверка
docker compose logs -f
```

**Готово!** Откройте http://ваш-сервер:5000

### Вариант 2: Используя готовый скрипт

Создайте `deploy.sh`:

```bash
#!/bin/bash

echo "🚀 Развертывание Severen Generator..."

# Создание структуры
mkdir -p ~/severen-app/{output,excel_files,data,email_templates}
cd ~/severen-app

# Скачивание конфигурации
curl -O https://raw.githubusercontent.com/ваш-username/severen-document-generator/main/docker-compose.prod.yml
mv docker-compose.prod.yml docker-compose.yml

# Создание .env если не существует
if [ ! -f .env ]; then
    echo "📝 Создание .env файла..."
    cat > .env << 'EOF'
DOCKERHUB_USERNAME=ваш-dockerhub-username
TRELLO_API_KEY=your_api_key_here
TRELLO_TOKEN=your_token_here
TRELLO_BOARD_ID=gfcln6rS
EXCEL_PATH=/app/excel_files/ПРИЛОЖЕНИЕ_1_3_4_ALL_IN_ONE.xlsx
WEB_PORT=5000
FLASK_ENV=production
SECRET_KEY=$(openssl rand -hex 32)
EOF
    echo "⚠️  Отредактируйте .env файл перед запуском!"
    exit 1
fi

# Запуск
echo "📦 Загрузка образа из Docker Hub..."
docker compose pull

echo "🚀 Запуск контейнеров..."
docker compose up -d

echo "✅ Готово! Приложение доступно на http://localhost:5000"
```

Использование:
```bash
chmod +x deploy.sh
./deploy.sh
```

---

## 🔄 Обновление приложения

### При обновлении кода в GitHub:

```bash
# На сервере:
cd ~/severen-app

# 1. Остановка текущей версии
docker compose down

# 2. Загрузка нового образа
docker compose pull

# 3. Запуск новой версии
docker compose up -d

# 4. Проверка логов
docker compose logs -f web
```

### Автоматизация обновления (webhook)

Создайте `update.sh`:

```bash
#!/bin/bash
cd ~/severen-app
docker compose pull
docker compose up -d
```

Настройте webhook в GitHub:
1. Settings → Webhooks → Add webhook
2. Payload URL: http://ваш-сервер/webhook
3. Content type: application/json
4. Events: Just the push event

---

## 📊 Структура проекта на сервере

```
~/severen-app/
├── docker-compose.yml          ← Конфигурация (из GitHub)
├── .env                        ← Секреты (локально!)
│
├── output/                     ← Сгенерированные документы
├── excel_files/                ← Excel файлы
│   └── ПРИЛОЖЕНИЕ_1_3_4_ALL_IN_ONE.xlsx
├── data/                       ← База данных
└── email_templates/            ← Шаблоны (автосоздаются)
```

**Что хранится где:**

```
GitHub:
✓ Код (Python, scripts)
✓ Dockerfile
✓ docker-compose.yml (dev + prod)
✓ Документация
✓ .gitignore
✓ GitHub Actions

Docker Hub:
✓ Собранные образы
✓ Версии (tags)

Сервер:
✓ .env (секреты)
✓ Excel файлы
✓ Сгенерированные документы
✓ База данных

GitHub Secrets:
✓ DOCKERHUB_USERNAME
✓ DOCKERHUB_TOKEN
✓ (Опционально) Trello ключи для CI
```

---

## 🔐 Безопасность

### Что НИКОГДА не коммитить в GitHub:

```
❌ .env файл
❌ Trello API ключи
❌ Пароли
❌ Секретные токены
❌ Excel файлы с данными
❌ Приватные ключи
```

### Что безопасно хранить в GitHub:

```
✓ Исходный код
✓ .env.example (шаблон без секретов)
✓ Dockerfile
✓ docker-compose.yml
✓ Документация
✓ Тесты
```

### Проверка перед commit:

```bash
# Проверьте что .env не в git
git status

# Проверьте .gitignore
cat .gitignore | grep .env

# Проверьте что нет секретов
git diff | grep -i "api_key\|token\|secret"
```

---

## 📝 Пример workflow разработки

```
┌─────────────────────────────────────────────────────────────┐
│  ЕЖЕДНЕВНАЯ РАЗРАБОТКА                                      │
└─────────────────────────────────────────────────────────────┘

1. Локальная разработка
   ─────────────────────
   • Пишете код
   • Тестируете: docker compose up -d
   • Проверяете: http://localhost:5000

2. Commit и Push
   ──────────────
   git add .
   git commit -m "Добавлена новая функция"
   git push origin main

3. Автоматическая сборка
   ──────────────────────
   • GitHub Actions запускается автоматически
   • Собирается Docker образ
   • Публикуется в Docker Hub
   • Время: 3-5 минут

4. Деплой на сервер
   ─────────────────
   ssh ваш-сервер
   cd ~/severen-app
   docker compose pull
   docker compose up -d

5. Проверка
   ─────────
   curl http://ваш-сервер:5000
   ✓ Работает!
```

---

## 🎯 Версионирование

### Семантическое версионирование (рекомендуется)

```bash
# Создание версии
git tag -a v1.0.0 -m "Release 1.0.0"
git push origin v1.0.0

# GitHub Actions автоматически создаст образы:
# • username/severen-generator:v1.0.0
# • username/severen-generator:1.0
# • username/severen-generator:1
# • username/severen-generator:latest
```

### Использование конкретной версии

В `docker-compose.yml`:

```yaml
services:
  web:
    image: username/severen-generator:v1.0.0  # Конкретная версия
    # или
    image: username/severen-generator:latest  # Последняя
```

---

## 📚 Полезные команды

### На локальной машине:

```bash
# Проверка перед push
git status
git diff

# Push с проверкой
git add .
git commit -m "Описание изменений"
git push origin main

# Просмотр Actions в GitHub
open https://github.com/ваш-username/severen-document-generator/actions
```

### На сервере:

```bash
# Обновление приложения
docker compose pull && docker compose up -d

# Просмотр логов
docker compose logs -f
docker compose logs -f web

# Проверка статуса
docker compose ps

# Перезапуск
docker compose restart web

# Полная остановка
docker compose down

# Очистка старых образов
docker system prune -a
```

---

## 🐛 Решение проблем

### GitHub Actions не запускается

**Проблема:** Workflow не выполняется после push

**Решение:**
1. Проверьте Actions включены: Settings → Actions → Allow all actions
2. Проверьте файл: `.github/workflows/docker-publish.yml` существует
3. Проверьте синтаксис YAML: https://www.yamllint.com/

### Ошибка "Access denied" в Docker Hub

**Проблема:** Cannot push to Docker Hub

**Решение:**
1. Проверьте Secrets: DOCKERHUB_USERNAME и DOCKERHUB_TOKEN
2. Проверьте права токена: Read, Write, Delete
3. Проверьте репозиторий существует в Docker Hub

### Образ не скачивается на сервере

**Проблема:** `docker compose pull` выдает ошибку

**Решение:**
1. Проверьте имя образа в docker-compose.yml
2. Для приватных репозиториев: `docker login`
3. Проверьте образ существует: `docker search username/severen-generator`

---

## 🎉 Готово!

Теперь у вас настроен полный CI/CD pipeline:

```
✓ Код безопасно хранится в GitHub
✓ Секреты в GitHub Secrets и локальном .env
✓ Автоматическая сборка при каждом push
✓ Образы публикуются в Docker Hub
✓ Простое развертывание на любом сервере
✓ Легкое обновление приложения
```

**Экономия времени:**
- Деплой: 30 секунд (вместо 30 минут)
- Обновление: 1 команда (вместо пересборки на сервере)
- Масштабирование: просто запустите на новом сервере

**Следующие шаги:**
1. Создайте GitHub репозиторий
2. Настройте Docker Hub
3. Добавьте Secrets
4. Push код → автоматическая сборка!
5. Разверните на сервере

**Удачи!** 🚀
