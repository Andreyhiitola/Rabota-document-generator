# 📦 ИНСТРУКЦИЯ ПО РАЗВЕРТЫВАНИЮ НА VPS

## 🎯 ЧТО БЫЛО СОЗДАНО

```
✅ sync_trello_severen.py      - Парсер Trello → Excel (400+ строк)
✅ dropbox_sync.py              - Интеграция с Dropbox
✅ full_sync.py                 - Главный скрипт синхронизации
✅ requirements_full.txt        - Зависимости Python
✅ .env.example                 - Пример конфигурации
✅ docker-compose.prod.yml      - Docker Compose с auto-sync
✅ README.md                    - Полная документация
```

---

## 🚀 ПОШАГОВОЕ РАЗВЕРТЫВАНИЕ

### ШАГ 1: Загрузка файлов на VPS

**На локальном компьютере:**

```bash
# Скачайте все файлы из Claude
# Затем загрузите на VPS:

scp sync_trello_severen.py root@ваш_vps:/root/severen-app/
scp dropbox_sync.py root@ваш_vps:/root/severen-app/
scp full_sync.py root@ваш_vps:/root/severen-app/
scp requirements_full.txt root@ваш_vps:/root/severen-app/
scp docker-compose.prod.yml root@ваш_vps:/root/severen-app/
scp README.md root@ваш_vps:/root/severen-app/
```

**ИЛИ через GitHub (рекомендуется):**

```bash
cd ~/severen-app

# Скопируйте файлы в репозиторий
cp /путь/к/скачанным/файлам/* .

# Загрузите в GitHub
git add .
git commit -m "feat: добавлена Dropbox синхронизация"
git push origin main
```

---

### ШАГ 2: Настройка переменных окружения

**На VPS:**

```bash
cd ~/severen-app
nano .env
```

**Добавьте в .env:**

```env
# DROPBOX (НОВОЕ!)
DROPBOX_TOKEN=sl.u.AGQUb9J9pLfdFy2wOPevy1MB8j5VIXS9yg7RqJlnQBCsnEN-6ZloDiypqxao5EmR...
DROPBOX_FILE_PATH=
EXCEL_FILE_PATH=/app/excel_files/workbook.xlsx
SYNC_INTERVAL=60

# TRELLO (уже есть)
TRELLO_API_KEY=ваш_ключ
TRELLO_TOKEN=ваш_токен
TRELLO_BOARD_ID=gfcln6rS
```

**Сохраните:** `Ctrl+O`, `Enter`, `Ctrl+X`

---

### ШАГ 3: Обновление через GitHub Actions

**Если используете GitHub:**

```bash
# GitHub Actions автоматически:
# 1. Соберёт новый Docker образ
# 2. Загрузит в Docker Hub
# 3. Watchtower обновит на VPS через 5 минут
```

**Проверьте статус:**
- https://github.com/Andreyhiitola/Rabota-document-generator/actions

**ИЛИ вручную:**

```bash
cd ~/severen-app
docker compose -f docker-compose.prod.yml down
docker compose -f docker-compose.prod.yml pull
docker compose -f docker-compose.prod.yml up -d web
```

---

### ШАГ 4: Тестовый запуск

```bash
cd ~/severen-app

# Запустите тестовую синхронизацию
docker compose -f docker-compose.prod.yml run --rm sync
```

**Ожидаемый результат:**

```
================================================================================
🚀 ЗАПУСК ПОЛНОЙ СИНХРОНИЗАЦИИ
================================================================================
Время: 2026-01-22 20:00:00

================================================================================
ШАГ 1/3: СКАЧИВАНИЕ ИЗ DROPBOX
================================================================================
✅ Подключен к Dropbox
✅ Найден файл: /Рабочие_таб_СЕВЕРЕН.xlsx
✅ Файл скачан успешно (245.3 KB)

================================================================================
ШАГ 2/3: СИНХРОНИЗАЦИЯ TRELLO → EXCEL
================================================================================
Загрузка карточек Trello...
Найдено карточек: 15
✅ Обработано карточек: 15
✅ Таблица сохранена

================================================================================
ШАГ 3/3: ЗАГРУЗКА В DROPBOX
================================================================================
✅ Файл загружен успешно (246.1 KB)

================================================================================
✅ ПОЛНАЯ СИНХРОНИЗАЦИЯ ЗАВЕРШЕНА УСПЕШНО!
================================================================================
```

---

### ШАГ 5: Запуск автосинхронизации

```bash
cd ~/severen-app

# Запустите автосинхронизацию (каждый час)
docker compose -f docker-compose.prod.yml --profile auto-sync up -d
```

**Проверьте статус:**

```bash
docker compose -f docker-compose.prod.yml ps
```

**Должны увидеть:**

```
NAME                 STATUS    PORTS
severen-web          Up        0.0.0.0:5000->5000/tcp
severen-auto-sync    Up
```

---

### ШАГ 6: Проверка логов

```bash
# Логи в реальном времени
docker compose -f docker-compose.prod.yml logs -f auto-sync

# Последние 50 строк
docker compose -f docker-compose.prod.yml logs --tail=50 auto-sync

# Файл логов
tail -f /tmp/full_sync.log
```

---

## ✅ ПРОВЕРКА РАБОТЫ

### Тест 1: Создайте карточку в Trello

```
Название:
гор. Санкт-Петербург, Тестовая ул., д.1. Номер работы: 99999

Описание:
Начало работ: 22.01.2026
Подрядчик: ООО Тест
Заказчик: ПАО "Ростелеком"

Метки:
🟦 Московский
🟩 Ростелеком
🟨 Пункт 3 (7050)
```

### Тест 2: Подождите 5-10 минут

```bash
# Или запустите вручную
docker compose run --rm sync
```

### Тест 3: Проверьте Excel в Dropbox

Откройте файл из Dropbox и найдите новую строку с работой №99999!

---

## 🐛 УСТРАНЕНИЕ ПРОБЛЕМ

### Проблема 1: "Файл не найден в Dropbox"

**Решение:**

```bash
nano .env
```

Укажите точный путь:

```env
DROPBOX_FILE_PATH=/путь/к/файлу.xlsx
```

### Проблема 2: "Ошибка авторизации Dropbox"

**Решение:**

1. Проверьте токен в `.env`
2. Убедитесь что токен не обрезан
3. Сгенерируйте новый токен на https://www.dropbox.com/developers/apps

### Проблема 3: "Контейнер не запускается"

**Решение:**

```bash
# Проверьте логи
docker compose logs sync

# Проверьте переменные окружения
cat .env

# Пересоберите образ
docker compose pull
docker compose up -d
```

### Проблема 4: "Python модули не найдены"

**Решение:**

Убедитесь что `requirements_full.txt` включён в Docker образ:

```dockerfile
# В Dockerfile должно быть:
COPY requirements_full.txt .
RUN pip install --no-cache-dir -r requirements_full.txt
```

---

## 📊 МОНИТОРИНГ

### Проверка статуса

```bash
# Статус контейнеров
docker compose ps

# Использование ресурсов
docker stats

# Логи последней синхронизации
tail -100 /tmp/full_sync.log
```

### Ручной запуск

```bash
# Однократная синхронизация
docker compose run --rm sync

# Перезапуск автосинхронизации
docker compose restart auto-sync
```

---

## 🔧 ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ

### Изменить интервал синхронизации

```bash
nano .env
```

```env
# Каждые 30 минут
SYNC_INTERVAL=30

# Каждые 2 часа
SYNC_INTERVAL=120
```

```bash
docker compose restart auto-sync
```

### Отключить автосинхронизацию

```bash
docker compose --profile auto-sync down
```

Использовать только ручной запуск:

```bash
docker compose run --rm sync
```

---

## 📝 ЧЕКЛИСТ РАЗВЕРТЫВАНИЯ

```
☐ 1. Загрузить файлы на VPS
☐ 2. Добавить DROPBOX_TOKEN в .env
☐ 3. Обновить Docker образ (GitHub или вручную)
☐ 4. Тестовый запуск: docker compose run --rm sync
☐ 5. Проверить результат в Dropbox
☐ 6. Запустить auto-sync: docker compose --profile auto-sync up -d
☐ 7. Проверить логи: docker compose logs -f auto-sync
☐ 8. Создать тестовую карточку в Trello
☐ 9. Подождать 1 час (или запустить вручную)
☐ 10. Проверить обновленный Excel в Dropbox
```

---

## 🎉 ГОТОВО!

Система развёрнута и работает!

**Автоматическая синхронизация каждый час!**

**Экономия времени: 30-60 минут в день!**

---

## 📞 ПОДДЕРЖКА

**Логи:**
```bash
docker compose logs -f auto-sync
tail -f /tmp/full_sync.log
tail -f /tmp/trello_sync.log
```

**Перезапуск:**
```bash
docker compose restart auto-sync
```

**Полная переустановка:**
```bash
docker compose down
docker compose pull
docker compose up -d web
docker compose --profile auto-sync up -d
```

---

**Версия:** 2.0  
**Дата:** 22.01.2026
