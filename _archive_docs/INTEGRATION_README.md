# 🔄 ИНТЕГРАЦИЯ НОВОЙ СИСТЕМЫ ОТЧЕТОВ

## ✅ Что обновлено в проекте

### Новые файлы:
1. **Рабочие_табл_СМР_v2.xlsx** - обновленная структура таблицы
2. **report_generator_v2.py** - новый генератор отчетов
3. **README_v2.md** - документация новой версии
4. **docker-compose.yml** - обновленный (с report-generator сервисом)
5. **Dockerfile** - обновленный
6. **generate_report.sh** - скрипт быстрой генерации

### Сохранено:
- Все существующие Python скрипты
- Синхронизация с Trello (sync_trello_severen.py)
- Веб-интерфейс (web_app.py)
- GUI приложение (gui_app.py, gui_app_with_trello.py)

---

## 🚀 Установка обновлений

### 1. Остановите текущие контейнеры

```bash
cd ~/Desktop/Rabota-document-generator
docker-compose down
```

### 2. Обновите файлы

Файлы уже созданы в проекте:
- ✓ Рабочие_табл_СМР_v2.xlsx
- ✓ report_generator_v2.py
- ✓ README_v2.md

### 3. Создайте директории (если нет)

```bash
mkdir -p output logs
```

### 4. Пересоберите контейнеры

```bash
docker-compose build --no-cache
docker-compose up -d
```

### 5. Проверьте статус

```bash
docker-compose ps
```

Должны работать 2 контейнера:
- severen-document-generator (веб-интерфейс на :5000)
- severen-report-generator (генератор отчетов)

---

## 📊 Использование новой системы отчетов

### Вариант 1: Через скрипт (рекомендуется)

```bash
# Сделайте скрипт исполняемым
chmod +x generate_report.sh

# Генерация отчета
./generate_report.sh 1 2026                    # За январь 2026
./generate_report.sh 1 2026 "СТОЛОТО"          # Для клиента
./generate_report.sh 1 2026 "" "Выполнен"      # Только выполненные
./generate_report.sh 1 2026 "" "" "Орбита"     # Исполнитель
```

### Вариант 2: Напрямую через Docker

```bash
docker-compose exec report-generator python3 report_generator_v2.py \
    --source /app/files/Рабочие_табл_СМР_v2.xlsx \
    --output /app/output/отчет_01_2026.xlsx \
    --month 1 \
    --year 2026
```

### Вариант 3: Через веб-интерфейс

```bash
# Откройте браузер
http://localhost:5000

# Там будет интерфейс для:
# - Синхронизации с Trello
# - Генерации отчетов
# - Просмотра данных
```

---

## 🔄 Работа с обеими таблицами

У вас теперь 2 системы:

### Старая система (сохранена):
- Файлы в `files/` директории
- Генератор: `report_generator.py` (старый)
- Синхронизация: `sync_trello_severen.py`

### Новая система:
- Файл: `Рабочие_табл_СМР_v2.xlsx`
- Генератор: `report_generator_v2.py` (новый)
- Улучшенная структура с 15 колонками

---

## 📝 Миграция данных

Если хотите перенести данные из старой таблицы в новую:

```bash
# Войдите в контейнер
docker-compose exec report-generator bash

# Запустите миграцию (нужно создать скрипт)
python3 migrate_data.py \
    --old files/старая_таблица.xlsx \
    --new files/Рабочие_табл_СМР_v2.xlsx
```

---

## 🛠️ Полезные команды

```bash
# Просмотр логов генератора отчетов
docker-compose logs -f report-generator

# Просмотр логов веб-приложения
docker-compose logs -f app

# Перезапуск контейнеров
docker-compose restart

# Вход в контейнер генератора
docker-compose exec report-generator bash

# Вход в контейнер веб-приложения
docker-compose exec app bash

# Проверка файлов в контейнере
docker-compose exec report-generator ls -la /app/files/

# Копирование файлов из контейнера
docker cp severen-report-generator:/app/output/отчет.xlsx ./
```

---

## 📚 Документация

- **README_v2.md** - основная документация новой системы
- **README_REPORTS.md** - документация по отчетам (старая)
- **README.md** - основной README проекта

---

## ⚠️ Важно

1. Новая таблица **Рабочие_табл_СМР_v2.xlsx** использует новую структуру с 15 колонками
2. Старая система продолжает работать параллельно
3. Можно использовать обе системы одновременно
4. Для новых отчетов используйте `report_generator_v2.py`

---

## 🆘 Решение проблем

### Контейнер не запускается

```bash
docker-compose logs report-generator
docker-compose build --no-cache
docker-compose up -d
```

### Файл не найден

```bash
# Проверьте наличие файла
ls -la Рабочие_табл_СМР_v2.xlsx

# Проверьте в контейнере
docker-compose exec report-generator ls -la /app/files/
```

### Нет прав на output

```bash
chmod -R 777 output logs
```

---

**Версия:** 2.0  
**Дата:** 22.01.2026  
**Статус:** Интегрировано в существующий проект
