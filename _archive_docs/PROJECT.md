# PROJECT SUMMARY: Severen Document Generator

**Дата:** 23 января 2026  
**Статус:** В разработке - исправление report_generator.py

---

## 🎯 ЦЕЛЬ ПРОЕКТА

Автоматизация генерации отчётов (ПРИЛОЖЕНИЕ 1, 3, 4) из рабочей таблицы Excel, 
синхронизируемой с Trello и Dropbox.

---

## 📂 СТРУКТУРА ПРОЕКТА

### **Репозиторий GitHub:**
```
https://github.com/Andreyhiitola/Rabota-document-generator
```

### **VPS Сервер:**
```
IP: 85.192.40.138
Путь: /root/severen-app
```

### **Контейнеры Docker:**
1. `severen-document-generator` - Web интерфейс (порт 5000)
2. `severen-report-generator` - Фоновые задачи
3. `severen-auto-sync` - Синхронизация Dropbox

---

## 📊 ДАННЫЕ

### **Исходная таблица:**

**Dropbox:**
```
/Рабочие таб СЕВЕРЕН _2026_Новый_форма.xlsx
```

**На VPS:**
```
/app/files/Рабочие_табл_СМР_v2.xlsx
```

**Локально (Fedora):**
```
~/Dropbox/Рабочие таб СЕВЕРЕН _2026_Новый_форма.xlsx
```

### **Структура листа "Работы":**

| № | Колонка | Описание |
|---|---------|----------|
| 1 | Скрыть (x) | Флаг скрытия |
| 2 | Акты на закрытие 1 | Тип акта |
| 3 | Акты на закрытие 2 | Статус акта |
| 4 | **Адрес + Задание** | **Основной адрес работ** |
| 5 | **Начало работ** | **Дата начала** |
| 6 | **Конец работ** | **Дата окончания** |
| 7 | **Клиент** | **Название клиента** |
| 8 | Исполнитель (ПО) | Подрядчик |
| 9 | Статус | Статус работы |
| 10 | **Название работ** | **Вид услуги** |
| 11 | Стоимость (руб) | Цена |
| 12 | Дата формирования отчета | Дата |
| 13 | **Транзитные адреса** | **Дополнительные адреса** |
| 14 | Примечание | Заметки |
| 15 | Описание из Trello | Архив |

---

## 🔧 ТЕКУЩИЙ КОД

### **1. web_app.py** ✅ РАБОТАЕТ

**Путь:** `~/Desktop/Rabota-document-generator/web_app.py`

**Текущая версия:**
```python
EXCEL_FILE = '/app/files/Рабочие_табл_СМР_v2.xlsx'
```

**Интерфейс:**
- 📅 Месяц (выпадающий список)
- 📅 Год (выпадающий список)
- 👤 Клиент (текстовое поле)
- [Сгенерировать отчёт]

**URL:** http://85.192.40.138:5000

**Статус:** ✅ Загружается, ✅ Файл найден, ❌ Отчёт кривой

---

### **2. report_generator.py** ❌ ТРЕБУЕТ ИСПРАВЛЕНИЯ

**Проблемы:**

#### **A. Неправильные номера колонок:**

**СЕЙЧАС (НЕВЕРНО):**
```python
COL_ADDR = 5     # E
COL_START = 26   # Z
COL_CLIENT = 22  # V
COL_PRICE = 9    # I
COL_SERVICE = 10 # J
COL_STATUS = 18  # R
COL_NUM = 1      # A
```

**ДОЛЖНО БЫТЬ:**
```python
COL_ADDR = 4     # D - Адрес + Задание
COL_START = 5    # E - Начало работ
COL_END = 6      # F - Конец работ
COL_CLIENT = 7   # G - Клиент
COL_SERVICE = 10 # J - Название работ
COL_PRICE = 11   # K - Стоимость (руб)
COL_TRANSIT = 13 # M - Транзитные адреса
COL_STATUS = 9   # I - Статус
```

#### **B. Логика транзитных адресов:**

**СЕЙЧАС:** Парсит текст адреса, ищет "Транзитные адреса:"

**ДОЛЖНО:** Читать из колонки M (13)

#### **C. Генерация отчёта:**

**Проблемы:**
1. Дата начала попадает в колонку "Адрес"
2. Транзитные адреса не отображаются
3. Выпадающий список услуг не работает

---

## 📋 ЗАДАЧИ НА ИСПРАВЛЕНИЕ

### **ПРИОРИТЕТ 1: Исправить report_generator.py**

1. **Изменить номера колонок:**
   ```python
   def extract_data(self, start_date: str = None, end_date: str = None) -> List[Dict]:
       # ...
       COL_ADDR = 4     # D - Адрес + Задание
       COL_START = 5    # E - Начало работ
       COL_END = 6      # F - Конец работ
       COL_CLIENT = 7   # G - Клиент
       COL_SERVICE = 10 # J - Название работ
       COL_PRICE = 11   # K - Стоимость
       COL_TRANSIT = 13 # M - Транзитные адреса
       COL_STATUS = 9   # I - Статус
   ```

2. **Исправить извлечение транзитов:**
   ```python
   transit_text = self.source_ws.cell(row_idx, COL_TRANSIT).value
   if transit_text:
       work_data['transit_addresses'] = [
           addr.strip() 
           for addr in str(transit_text).split(',') 
           if addr.strip()
       ]
   ```

3. **Исправить generate_report():**
   - Адрес → колонка 1
   - Дата начала → колонка 2
   - Дата конца → колонка 3
   - Вид услуги → колонка 4
   - Транзиты отдельными строками с объединением ячеек

4. **Починить выпадающий список:**
   ```python
   services_list = [
       "Прокладка ВОК",
       "Прокладка оптоволокна",
       "Установка оптической муфты",
       # ...
   ]
   dv = DataValidation(type="list", formula1='"' + ','.join(services_list) + '"')
   ```

### **ПРИОРИТЕТ 2: Улучшить web_app.py**

**Добавить поля:**
- 📅 Дата начала отчета (input type="date")
- 📅 Дата конца отчета (input type="date")
- 🔢 Номер задания (input type="text")

**Заменить:**
```python
# БЫЛО:
month = data.get('month')
year = data.get('year')

# СТАЛО:
start_date = data.get('start_date')
end_date = data.get('end_date')
task_number = data.get('task_number')
```

---

## 🔄 WORKFLOW СИНХРОНИЗАЦИИ

```
Trello (доски с карточками)
    ↓
sync_trello_severen.py
    ↓
Excel (Рабочие_табл_СМР_v2.xlsx)
    ↓
dropbox_sync.py
    ↓
Dropbox (/Рабочие таб СЕВЕРЕН _2026_Новый_форма.xlsx)
    ↓
severen-auto-sync контейнер
    ↓
/app/files/Рабочие_табл_СМР_v2.xlsx (на VPS)
    ↓
report_generator.py → ПРИЛОЖЕНИЕ 1, 3, 4
    ↓
web_app.py → Скачивание пользователем
```

---

## 🚀 ДЕПЛОЙ

### **Локальная машина (Fedora):**

```bash
cd ~/Desktop/Rabota-document-generator

# Внести изменения в файлы
nano report_generator.py
nano web_app.py

# Закоммитить
git add .
git commit -m "Исправлены номера колонок и логика генерации отчётов"
git push
```

### **VPS:**

```bash
ssh root@85.192.40.138
cd ~/severen-app

# Обновить код
git pull

# Пересобрать контейнеры
docker-compose down
docker-compose build --no-cache
docker-compose up -d

# Проверить
docker logs severen-document-generator --tail=50
```

### **Проверка:**

Открыть: http://85.192.40.138:5000

---

## 📝 ПРИМЕЧАНИЯ

### **Переменные окружения (severen-auto-sync):**

```bash
DROPBOX_FILE_PATH=/Рабочие таб СЕВЕРЕН _2026_Новый_форма.xlsx
DROPBOX_TOKEN=sl.u.AGRW-MVm_9wF17u6EIffzCw... (длинный токен)
```

### **Git ветки:**

- **Локально:** main
- **На VPS:** master → origin/main

Связка настроена:
```bash
git branch --set-upstream-to=origin/main master
```

### **Docker Watchtower:**

❌ Не используется (можно настроить позже для автоматических обновлений)

---

## ✅ ЧТО РАБОТАЕТ

- ✅ Web интерфейс загружается
- ✅ Excel файл найден и читается
- ✅ Синхронизация Dropbox ↔ VPS
- ✅ Синхронизация Trello → Excel
- ✅ Генерация отчёта запускается

## ❌ ЧТО НЕ РАБОТАЕТ

- ❌ Неправильные номера колонок в report_generator.py
- ❌ Дата начала попадает в колонку "Адрес"
- ❌ Транзитные адреса не отображаются
- ❌ Выпадающий список услуг не работает
- ❌ Web форма не имеет полей для дат начала/конца

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. ✅ Создать PROJECT.md (этот файл)
2. ⏳ Исправить report_generator.py (номера колонок)
3. ⏳ Исправить логику транзитных адресов
4. ⏳ Исправить generate_report() (правильная таблица)
5. ⏳ Добавить поля дат в web_app.py
6. ⏳ Протестировать на реальных данных
7. ⏳ Задеплоить на VPS

---

**Последнее обновление:** 23 января 2026, 10:10 MSK
