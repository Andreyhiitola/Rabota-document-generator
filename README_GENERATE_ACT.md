# 📄 ГЕНЕРАТОР АКТОВ - generate_act.py

## 🎯 Что делает

Автоматически генерирует готовый акт выполненных работ на основе:
- Данных из **data.xlsx** (лист "Работы")
- Шаблона **template.xlsx**

---

## 🚀 Использование

### Базовая команда:
```bash
python3 generate_act.py --data data.xlsx --template template.xlsx
```

### На VPS с Docker:
```bash
docker compose -f docker-compose.prod.yml run --rm \
  -v $(pwd):/app \
  sync python3 generate_act.py --data /app/excel_files/data.xlsx --template /app/templates/template.xlsx
```

---

## 📊 Структура данных

### Файл data.xlsx должен содержать:

#### Лист "Работы" с колонками:
```
A: Номер акта          ← ВАЖНО! Берётся для группировки
B: Дата закрытия акта
C: Адрес + Задание
D: Начало работ
E: Конец работ
F: Название работ      ← Используется для определения стоимости
G: Стоимость (руб)
...
K: Статус              ← Фильтр по "Выполнен"
```

#### Лист "Справочник_Работы" (расценки):
```
A: Код (1, 2, 3, ...)
B: Название работы
C: Стоимость в рублях
```

Пример:
```
1 | 1. Консультации по размещению кабелей ВОЛС             | 1850
2 | 2. Согласование работ по кабельной трассе с ЖКС/ГУПРЭП | 5250
3 | 3. Согласование работ по кабельной трассе с ТСЖ/УК     | 7050
...
```

---

## 📋 Шаблон template.xlsx

### Структура шаблона:

#### Лист "Задание_Отчет_Форма_1-3":

**Строка 5, колонка C:** Номер акта (заполняется автоматически)

**Строки 13+:** Таблица с данными

```
ЗАДАНИЕ (левая часть):
├─ A: Адрес
├─ B: Дата передачи задания
├─ C: Дата выполнения задания
└─ D: Вид оказанной услуги

ОТЧЕТ (правая часть):
├─ G: Адрес
├─ H: Дата передачи задания
├─ I: Дата выполнения задания
├─ J: Вид оказанной услуги
└─ K: Стоимость (руб)
```

---

## ⚙️ Логика работы

### 1. Загрузка данных
```
data.xlsx → Лист "Работы"
         → Лист "Справочник_Работы"
```

### 2. Определение номера акта
```
Берётся из столбца "Номер акта" (первое непустое значение)
Нормализуется: "1.0" → "1", "01-1" → "1"
```

### 3. Фильтрация работ
```
Условия:
├─ Статус содержит "Выполнен"
├─ Номер акта совпадает
├─ Начало работ заполнено
└─ Конец работ заполнен
```

### 4. Расчёт стоимости
```
Для каждой работы:
├─ Если есть "Стоимость (руб)" → берём её
└─ Иначе ищем код в "Название работ" (1., 2., 3., ...)
   └─ Берём цену из справочника
```

### 5. Заполнение шаблона
```
template.xlsx:
├─ C5 ← "№ {номер_акта}"
├─ Строки 13+ ← Данные работ
├─ K{последняя_строка+1} ← Формула суммы
└─ J{последняя_строка+2} ← Сумма прописью
```

### 6. Сохранение
```
Готовый_Акт_{месяц}_{дата-время}.xlsx
```

---

## 💡 Примеры

### Пример 1: Простой случай
```bash
python3 generate_act.py \
  --data data.xlsx \
  --template template.xlsx
```

**Результат:**
```
🔄 Загрузка данных...
📋 Найдено листов: 6
  Месяц: январь 2026
  Фильтр статуса: Выполнен
📊 Данные работ: 'Работы'
  Загружено строк: 50
  Номер акта (из столбца A): 2
  Отфильтровано строк: 8
  Период работ: 01.01.2026 - 30.01.2026
💰 Расценки: 'Справочник_Работы'
  Найдено расценок: 7
💵 ИТОГО: 42,000 руб.
  Пропись: 42000 (Сорок две тысячи) рублей, 00 копеек
📄 Заполнение шаблона...
  ✏️ Заполнены 8 строк(и) таблицы
  🔢 K21 = =СУММ(K$13:СМЕЩ(K21;-1;0))
  📝 J22 = 42000 (Сорок две тысячи) рублей, 00 копеек
  ✅ Задание_Отчет_Форма_1-3: готово!

🎉 ГОТОВО!
📁 Файл: Готовый_Акт_январь 2026_2026-01-22_18-30-00.xlsx
📋 Номер акта: 2
💰 Итого: 42000 (Сорок две тысячи) рублей, 00 копеек
📈 Работ: 8 шт.
```

---

## 🔧 Настройка

### Изменить месяц в имени файла:
В файле `generate_act.py` найдите строку:
```python
month_name = 'январь 2026'  # ← ИЗМЕНИТЕ ЗДЕСЬ
```

### Изменить фильтр статуса:
```python
status_filter = 'Выполнен'  # ← можно изменить на '4-этап', 'Завершено' и т.д.
```

### Изменить начальную строку данных:
```python
FIRST_DATA_ROW = 13  # ← если в шаблоне таблица начинается с другой строки
```

---

## 📦 Зависимости

```bash
pip install pandas openpyxl
```

Или в Docker (уже установлены):
```
pandas
openpyxl
python-dateutil
```

---

## 🔄 Интеграция с системой синхронизации

### В full_sync.py можно добавить:

```python
from generate_act import generate_act

def full_sync():
    # ... существующий код синхронизации ...
    
    # Генерация акта после синхронизации
    logger.info("=" * 80)
    logger.info("ШАГ 4/4: ГЕНЕРАЦИЯ АКТА")
    logger.info("=" * 80)
    
    try:
        generate_act(
            data_path='/app/excel_files/workbook.xlsx',
            template_path='/app/templates/template.xlsx'
        )
        logger.info("✅ Акт сгенерирован!")
    except Exception as e:
        logger.error(f"❌ Ошибка генерации акта: {e}")
```

---

## ⚠️ Важные замечания

### 1. Номер акта
```
Берётся из ПЕРВОГО непустого значения столбца "Номер акта"
└─ Все работы с этим номером попадут в один акт
```

### 2. Фильтр по статусу
```
Только работы со статусом "Выполнен" попадут в акт
└─ Если нужны другие статусы - измените в коде
```

### 3. Транзитные адреса
```
НЕ обрабатываются автоматически!
└─ Адрес берётся как есть из колонки "Адрес + Задание"
└─ Если нужно объединение ячеек - требуется доработка
```

### 4. Динамическая формула суммы
```
=СУММ(K$13:СМЕЩ(K{строка};-1;0))
└─ Автоматически считает все строки выше
└─ Не нужно менять формулу при добавлении/удалении строк
```

---

## 📊 Сравнение с report_generator.py

```
report_generator.py:
✅ Обработка транзитных адресов
✅ Объединение ячеек
✅ Выпадающие списки
✅ Более сложная логика
❌ Больше кода (~600 строк)

generate_act.py:
✅ Простота (~400 строк)
✅ Работает с готовым шаблоном
✅ Динамическая формула суммы
✅ Сумма прописью
❌ Нет обработки транзитных адресов
❌ Нет объединения ячеек
```

**Рекомендация:** Используйте `generate_act.py` для простых случаев без транзитных адресов.

---

## 🐛 Устранение проблем

### "Столбец 'Номер акта' не найден"
→ Проверьте что в data.xlsx есть колонка с точным названием "Номер акта"

### "Нет строк для акта X"
→ Проверьте:
  - Есть ли работы с этим номером акта
  - Есть ли статус "Выполнен"
  - Заполнены ли даты

### "Лист 'Задание_Отчет_Форма_1-3' не найден"
→ Проверьте название листа в template.xlsx

### "Не найдены расценки"
→ Проверьте структуру листа "Справочник_Работы":
  - Колонка A: коды (1, 2, 3, ...)
  - Колонка C: цены

---

## ✅ Чеклист использования

```
☐ data.xlsx подготовлен (14 колонок)
☐ Справочник_Работы заполнен
☐ template.xlsx настроен
☐ Установлены зависимости (pandas, openpyxl)
☐ Проверен номер акта в data.xlsx
☐ Проверен статус работ ("Выполнен")
☐ Запущен generate_act.py
☐ Проверен результат
```

---

**Версия:** 1.0  
**Дата:** 22.01.2026  
**Автор:** Система автоматизации "Северен"
