text
# 🪟 Инструкция для работы в Windows

## 📋 Требования

- Python 3.9+
- Установленный `curl` (обычно есть в Windows 10/11)

## 🚀 Первоначальная настройка

### 1. Установите зависимости

```cmd
pip install -r requirements_full.txt
2. Создайте .env файл
Скопируйте .env.example в .env и заполните:

text
TOKEN=ваш_dropbox_токен
TRELLO_API_KEY=ваш_trello_api_key
TRELLO_TOKEN=ваш_trello_token
TRELLO_BOARD_ID=ваш_board_id
📥 Работа с data.xlsx
Скачать свежий data.xlsx из Dropbox
Дважды кликните:

text
sync_download.bat
Или через командную строку:

text
python sync_data.py --download
Загрузить изменения обратно в Dropbox
Дважды кликните:

text
sync_upload.bat
Или через командную строку:

text
python sync_data.py --upload
📄 Генерация актов
Способ 1: Через .bat файл (рекомендуется)
Дважды кликните:

text
generate_act.bat
Способ 2: Через командную строку
text
python generate_act.py --data "data.xlsx" --template "template.xlsx"
🔄 Типичный рабочий процесс
Вариант А: Работа с данными
text
1. Запустите sync_download.bat     → Скачает свежий data.xlsx
2. Откройте data.xlsx в Excel       → Просмотрите/отредактируйте данные
3. Запустите sync_upload.bat        → Загрузит изменения в Dropbox
Вариант Б: Генерация акта
text
1. Запустите sync_download.bat     → Скачает свежий data.xlsx
2. Запустите generate_act.bat      → Создаст готовый акт
3. Найдите готовый файл:
   Готовый_Акт_январь 2026_ГГГГ-ММ-ДД_ЧЧ-ММ-СС.xlsx
🎯 Быстрый старт
Для генерации акта прямо сейчас:
sync_download.bat ← дважды кликните

generate_act.bat ← дважды кликните

Готово! Акт создан ✅

⚠️ Устранение проблем
"curl не найден"
Установите curl:

Windows 10/11: уже установлен

Или скачайте: https://curl.se/windows/

"Python не найден"
Установите Python:

Скачайте: https://www.python.org/downloads/

При установке отметьте "Add Python to PATH"

"Модуль не найден"
text
pip install -r requirements_full.txt
"Ошибка загрузки в Dropbox"
Проверьте .env:

Правильно ли указан TOKEN?

Есть ли права доступа?

📞 Поддержка
При проблемах проверьте:

Файл .env существует и заполнен

Интернет-соединение работает

Python установлен корректно

text

## Создайте все .bat файлы

**sync_download.bat**:
```batch
@echo off
chcp 65001 >nul
title Скачать data.xlsx
echo.
python sync_data.py --download
if %ERRORLEVEL% NEQ 0 (
    echo.
    echo Нажмите любую клавишу для выхода...
    pause >nul
)
sync_upload.bat:

text
@echo off
chcp 65001 >nul
title Загрузить data.xlsx
echo.
python sync_data.py --upload
if %ERRORLEVEL% NEQ 0 (
    echo.
    echo Нажмите любую клавишу для выхода...
    pause >nul
)
generate_act.bat (обновленный):

text
@echo off
chcp 65001 >nul
title Генерация акта
echo.
echo ================================================
echo 📄 ГЕНЕРАЦИЯ АКТА
echo ================================================
echo.
python generate_act.py --data "data.xlsx" --template "template.xlsx"
echo.
if %ERRORLEVEL% EQU 0 (
    echo ✅ Акт успешно создан!
) else (
    echo ❌ Ошибка! Проверьте data.xlsx и template.xlsx
)
echo.
echo Нажмите любую клавишу для выхода...
pause >nul
Теперь пользователь на Windows просто дважды кликает по .bat файлам!
