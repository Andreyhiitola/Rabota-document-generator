version: '3.8'
services:
  trello:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
  
  reports:
    build:
      context: .
      dockerfile: Dockerfile.reports
    ports:
      - "8080:8080"
    volumes:
      - ./templates:/app/templates:ro
      - /tmp:/tmp
